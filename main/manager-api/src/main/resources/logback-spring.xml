<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Kích hoạt JansiConsoleAppender để đảm bảo đầu ra console có màu -->
    <conversionRule conversionWord="clr" converterClass="org.springframework.boot.logging.logback.ColorConverter" />
    
    <!-- Đảm bảo thư mục nhật ký tồn tại -->
    <timestamp key="bySecond" datePattern="yyyyMMdd'T'HHmmss"/>
    
    <!-- Định nghĩa vị trí lưu trữ tệp nhật ký -->
    <property name="LOG_HOME" value="./logs" />
    
    <!-- Sử dụng trình lắng nghe khởi tạo tùy chỉnh để đảm bảo thư mục nhật ký tồn tại -->
    <define name="LOGBACK_DIR_CHECK" class="ch.qos.logback.core.property.FileExistsPropertyDefiner">
        <path>${LOG_HOME}</path>
        <createIfMissing>true</createIfMissing>
    </define>
    
    <!-- Nhập cấu hình mặc định của Spring Boot -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />
    
    <!-- Cấu hình đầu ra console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{50}) - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- Cấu hình đầu ra tệp - Tất cả nhật ký -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/xiaozhi-esp32-api.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/xiaozhi-esp32-api.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- Cấu hình đầu ra tệp - Chỉ nhật ký lỗi -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_HOME}/error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_HOME}/error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>
    
    <!-- Cấu hình môi trường phát triển -->
    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <!-- Môi trường phát triển cũng ghi lại tệp nhật ký -->
            <appender-ref ref="FILE" />
            <appender-ref ref="ERROR_FILE" />
        </root>
        <logger name="xiaozhi" level="DEBUG" />
        <logger name="org.springframework.web" level="INFO" />
        <logger name="org.springboot.sample" level="INFO" />
    </springProfile>
    
    <!-- Cấu hình môi trường kiểm thử -->
    <springProfile name="test">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="FILE" />
            <appender-ref ref="ERROR_FILE" />
        </root>
        <logger name="xiaozhi" level="INFO" />
        <logger name="org.springframework.web" level="INFO" />
        <logger name="org.springboot.sample" level="INFO" />
    </springProfile>
    
    <!-- Cấu hình môi trường sản xuất - Kết hợp các thực hành tốt nhất của hai phiên bản -->
    <springProfile name="prod">
        <!-- Sử dụng đường dẫn nhật ký cục bộ -->
        <appender name="PROD_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/xiaozhi-esp32-api.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/xiaozhi-esp32-api.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>10MB</maxFileSize>
                <maxHistory>30</maxHistory>
                <totalSizeCap>2GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>
        
        <!-- Nhật ký lỗi môi trường sản xuất -->
        <appender name="PROD_ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_HOME}/error.log</file>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>ERROR</level>
            </filter>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_HOME}/error.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>10MB</maxFileSize>
                <maxHistory>30</maxHistory>
                <totalSizeCap>1GB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>
        
        <!-- Cấu hình nhật ký gốc môi trường sản xuất -->
        <root level="INFO">
            <appender-ref ref="PROD_FILE" />
            <appender-ref ref="PROD_ERROR_FILE" />
            <appender-ref ref="CONSOLE" />
        </root>
        
        <!-- Mức độ nhật ký của các gói cụ thể -->
        <logger name="xiaozhi" level="INFO" />
        <logger name="org.springframework.web" level="ERROR" />
        <logger name="org.springboot.sample" level="ERROR" />
    </springProfile>
    
    <!-- Đảm bảo thư mục nhật ký tồn tại -->
    <contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
        <resetJUL>true</resetJUL>
    </contextListener>
</configuration>
